# Jena Climate forecasting - Python 3.10
#
# EN: PyTorch must be installed FIRST and from its own index, because the CUDA
#     build is not on PyPI. I use the cu126 wheels deliberately: my GPU is a
#     GTX 1050 Ti (Pascal, sm_61) and PyTorch's newer cu128 wheels no longer
#     compile for Pascal and need a 570+ driver. cu126 covers sm_50 to sm_90.
#
#       pip install torch==2.7.1 --index-url https://download.pytorch.org/whl/cu126
#       pip install -r requirements.txt
#
#     For CPU-only machines, drop the --index-url and just `pip install torch`.
#
# TR: PyTorch ÖNCE ve kendi index'inden kurulmalı, çünkü CUDA derlemesi PyPI'da
#     yok. cu126 wheel'lerini bilerek kullanıyorum: GPU'm bir GTX 1050 Ti
#     (Pascal, sm_61) ve PyTorch'un yeni cu128 wheel'leri artık Pascal için
#     derlenmiyor, ayrıca 570+ sürücü istiyor. cu126, sm_50-sm_90 arasını kapsıyor.
#
#       pip install torch==2.7.1 --index-url https://download.pytorch.org/whl/cu126
#       pip install -r requirements.txt
#
#     Yalnızca CPU olan makinelerde --index-url'ü at, düz `pip install torch` yeter.

torch==2.7.1

numpy==2.2.6
pandas==2.3.3
scipy==1.15.3
scikit-learn==1.7.2

matplotlib==3.10.9
seaborn==0.13.2

jupyter==1.1.1
jupyterlab==4.6.3
notebook==7.6.2
ipykernel==7.3.0

tqdm==4.70.1
